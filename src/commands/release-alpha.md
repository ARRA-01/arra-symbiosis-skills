---
description: [arra] v0.1.0 G-SKLL | Bump version, compile, test, commit, tag, and create GitHub alpha pre-release. Use when user says "release alpha", "alpha release", "new alpha", "tag alpha". Do NOT trigger for stable releases or version bumps without tagging.
argument-hint: "[--patch | --minor | --major]"
---

# /release-alpha

Execute the `release-alpha` skill with the provided arguments.

## Instructions

**If you have a Skill tool available**: Use it directly with `skill: "release-alpha"` instead of reading the file manually.

**Otherwise**:
1. Read the skill file at this exact path: `~/.claude/skills/release-alpha/SKILL.md`
2. Follow all instructions in the skill file
3. Pass these arguments to the skill: `$ARGUMENTS`

**WARNING**: Do NOT use Glob, find, or search for this skill. The path above is the ONLY correct location.

---
*🧬 ARRA Symbiosis Skills · v0.1.0*
